version: 2.1
jobs:
  hi_job:
    docker:
     - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: letsRunCI
          command: |
              echo "hi salah"

workflows:
  new_wokflow:
    jobs:
      - hi_job